function sumOfMultiples(a,b,l)
{
    const l = [0,1,2,3,4,5,10];
    const a = [2];
    const b = [5];
    sum=0;
    for(i = 0; i<l.length;i++)
    {
        if(l[i] % a ==0) || ( l[i] % b ==0)
        (
            <p>multiple of a and b.</p>
            )
    }
    
}
